# Semantic-Page-Search
Semantic Find is a Chrome extension that acts like a smarter Ctrl+F. Instead of searching for exact text matches, it uses embeddings to understand the meaning of a query and highlights the most relevant sections of a page, even if the wording is different.
